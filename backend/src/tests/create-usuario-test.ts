import { Usuario } from "../domain/usuario/entity/usuario";
import { BcryptHasher } from "../infra/services/bcrypt-hasher";

describe("Teste para login", () => {
    test("O usuário deve ser criado com senha hasheada", () => {
       
    });
    test("O usuário não deve ser criado", () => {

    });
});